You have to install node.js first.

You also have to install npm package manager.

Then you have to open a terminal in the folder of the project, then install there express (npm i express) and nodemon (npm i nodemon).

To run the project you have to put this command:
nodemon app.js